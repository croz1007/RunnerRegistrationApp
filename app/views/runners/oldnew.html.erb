  <h1>Myers Running Camp Registration</h1>

  <div class="formContainer">
    <h4>Runners Form</h4>
    <hr />
    <p>We appologize for the long form, but it allows us to collect everything we need for your registration. This way, the only thing you will be emailed before camp will be the instructions and directions to get to camp and what to bring! Thank you.</p>

    <p class="italic note">* Indicates Required Filed</p>


    <%= form_for @runner do |f| -%>

      First name: <%= f.text_field :firstname %><br />
      Last name: <%= f.text_field :lastname %><br />
      Address 1: <%= f.text_field :address1 %><br />
      Address 2: <%= f.text_field :address2 %><br />
      City: <%= f.text_field :city %><br />
      State: <%= f.text_field :state %><br />
      ZIP: <%= f.text_field :zip %><br />
      Phone: <%= f.telephone_field :phone %><br />
      School Name: <%= f.text_field :schoolname %><br />
      Gender: <%= f.text_field :gender %><br />
      Age: <%= f.text_field :age %><br />
      <%= f.submit %>

    <% end -%>
  </div>
  <%- if !@runners.blank? %>
    <div id="runnerList">
      <h4>Current Runners</h4>
      <hr />
        <%- @runners.each_with_index do |r, index| %>
          <div class="runnerBlock">
            <strong><%= link_to runner_name(r.firstname, r.lastname), runner_path(id: r.id) -%></strong>
            <hr />
            <%= r.address1 %></br>
            <%- if !r.address2.blank? %>
              <%= r.address2 %></br>
            <%- end %>
            <%= r.city %> <%= r.state %> <%= r.zip %></br>
            <%- if !r.phone.blank? %>
              <%= r.phone %></br>
            <%- end %>
            <%= r.schoolname %></br>
            Gender: <%= r.gender %><br />
            Age: <%= r.age %>
          </div>
          <%- if index+1 != @runners.size %>
            <hr />
          <%- end %>
        <%- end %>
    </div>
  <%- end %>
